# Copy to keystore.properties, which is gitignored, or let `mise run keystore`
# generate both the keystore and the real file.
#
# The keystore itself is kept outside the repo.
storeFile=/home/you/.android-keystores/countdown-release.jks
storePassword=
keyAlias=countdown
keyPassword=
